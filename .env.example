# Proveedor LLM: OpenAI. La clave es SOLO del backend, nunca en el front ni en el repo.
OPENAI_API_KEY=
# Modelo a usar (por defecto gpt-4o-mini para bajo costo).
OPENAI_MODEL=gpt-4o-mini
# Puerto del servidor.
PORT=3000
# Topes de control de costo.
MAX_ITERACIONES=25
MAX_TOKENS_SESION=100000
